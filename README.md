# Range Lab V4

Deploy on GitHub Pages:
1. Replace your repository root `index.html` with this `index.html`.
2. Commit/push to `main`.
3. In GitHub: Settings → Pages → Deploy from a branch → `main` / `(root)`.

V4 change: every logged shot triggers an 8-second full-screen reset ritual with rotating breathing, release, and pre-shot routine cues before the next club appears.
